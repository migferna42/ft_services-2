apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - hop

---

    apiVersion: v1
    kind: Service
    metadata:
      name: loadbalancer
    spec:
      selector:
        app: ft_services
      ports:
      - name: http
        protocol: TCP
        port: 80
        targetPort: 80
      - name: https
        protocol: TCP
        port: 443
        targetPort: 443
      type: LoadBalancer

# ---
